# Supabase — pegue em: Project Settings → Data API / API Keys
VITE_SUPABASE_URL=https://SEU-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key-publica

# URL pública do site (usada pelos apps Android/iOS nos links de e-mail de
# confirmação/recuperação — dentro do app a origem é localhost e não serve).
# Obrigatória ao buildar para mobile (npm run mobile:sync).
VITE_SITE_URL=https://seu-dominio.com.br

# ─────────────────────────────────────────────────────────────
# Secrets das Edge Functions (NÃO vão no frontend / build do Vite).
# Configure com: supabase secrets set NOME=valor
#
#   ASAAS_API_KEY              chave da API do ASAAS (checkout ativo)
#
#   Os PREÇOS não são secret: ficam no código, em PLAN_CONFIG na função
#   asaas-create-subscription (espelhando src/plans.js). Mudar preço = editar
#   os dois e rodar `supabase functions deploy asaas-create-subscription`.
#
#   Stripe é LEGADO (ver migration 20260711000000_stripe_legacy_only): não
#   recebe checkout novo, só mantém quem já assinava por lá. Os secrets abaixo
#   existem só para esses contratos antigos — os preços neles são os de quando
#   foram criados e não acompanham a tabela atual.
#   STRIPE_SECRET_KEY          chave secreta do Stripe
#   STRIPE_WEBHOOK_SECRET      signing secret do webhook
#   STRIPE_PRICE_ID / _ANNUAL / _DUO / _DUO_ANNUAL
#
#   OPENAI_API_KEY             chave secreta da OpenAI (assistente de IA)
#   OPENAI_MODEL               modelo opcional (padrão: gpt-4o-mini)
# ─────────────────────────────────────────────────────────────
